import { WithChildrenProps } from '@structures';
interface TileProps extends WithChildrenProps {
    isRound?: boolean;
}
export declare const Tile: import("styled-components").StyledComponent<"div", any, TileProps, never>;
export {};
