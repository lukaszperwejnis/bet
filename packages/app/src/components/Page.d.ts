import { RenderType } from '@structures';
interface PageProps {
    children: RenderType;
    centered?: boolean;
}
export declare const Page: import("styled-components").StyledComponent<"div", any, PageProps, never>;
export {};
