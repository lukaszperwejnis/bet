export declare const AppUrls: {
    LOGIN: {
        (options?: Partial<{
            params: Record<string, unknown>;
            query: string;
            hash: string;
            fragment: string;
        }>): string;
        pattern: string;
    };
    MAIL_INVITATION_SIGNUP: {
        (options?: Partial<{
            params: Record<string, unknown>;
            query: string;
            hash: string;
            fragment: string;
        }>): string;
        pattern: string;
    };
    INVALID_MAIL_INVITATION_SIGNUP_TOKEN: {
        (options?: Partial<{
            params: Record<string, unknown>;
            query: string;
            hash: string;
            fragment: string;
        }>): string;
        pattern: string;
    };
    RESET_PASSWORD: {
        (options?: Partial<{
            params: Record<string, unknown>;
            query: string;
            hash: string;
            fragment: string;
        }>): string;
        pattern: string;
    };
    SET_PASSWORD: {
        (options?: Partial<{
            params: Record<string, unknown>;
            query: string;
            hash: string;
            fragment: string;
        }>): string;
        pattern: string;
    };
    DASHBOARD: {
        (options?: Partial<{
            params: Record<string, unknown>;
            query: string;
            hash: string;
            fragment: string;
        }>): string;
        pattern: string;
    };
};
