import { PasswordStoreType } from '../types';
import { PasswordActions } from '../actions';
export declare const passwordReducer: (state: PasswordStoreType | undefined, action: PasswordActions) => PasswordStoreType;
