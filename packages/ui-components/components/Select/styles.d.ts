import { WithChildrenProps } from "../../types";
interface SelectProps extends WithChildrenProps {
    disabled?: boolean;
}
export declare const Select: import("styled-components").StyledComponent<"select", any, SelectProps, never>;
export {};
